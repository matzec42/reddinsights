// server actions
// function for saving an analysis (POST) in AnalyzerPage (/analyzer)
// function for updating user profile info like password (POST)

// DO NOT USE the function below --- just an experiment on how to connect a server action with the form (POST / user's search term from the AnalyzePage component).
// "use server"
// export async function getSubredditThreads(formData: FormData) {
//     const rawFormData = {
//         query: formData.get('query')
//     }

//     console.log(`Testing server action, log from getSubreddtThreads function: ${rawFormData.query}`);

// }